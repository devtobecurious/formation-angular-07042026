import { MaDirective } from './ma-directive';

describe('MaDirective', () => {
  it('should create an instance', () => {
    const directive = new MaDirective();
    expect(directive).toBeTruthy();
  });
});
