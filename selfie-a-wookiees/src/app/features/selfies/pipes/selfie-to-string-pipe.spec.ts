import { SelfieToStringPipe } from './selfie-to-string-pipe';

describe('SelfieToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SelfieToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
